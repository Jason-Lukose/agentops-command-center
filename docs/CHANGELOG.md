# Changelog

> One line per meaningful change, newest first. Updated at the end of every task (Phase 9), fix (Phase 12), and refactor (Phase 13). Haiku-friendly maintenance via `/update-docs`.

## Format
`- YYYY-MM-DD [feat|fix|refactor|docs|chore] <what changed> (task/bug ID)`

## Unreleased

- 2026-07-12 [feat] Intensified motion pass across all 6 pages (dial ~4/10 → ~6-7/10, "the site should feel alive"): extended `src/lib/motion.ts` with `springBouncy`, `rowContainer`/`rowItem`, `cardHover`/`cardTap`, `growBar`, `shakeOnce`, `checkDraw`, `glowPulse`, and a `useCountUp` hook (all transform/opacity-only, `useReducedMotion()`-gated, ≤500ms); `fadeRise` travel bumped 8px→12px. Dashboard: metric cards count up + hover-lift/press (`MetricCard.tsx`), recent-runs table rows cascade in and animate on refresh with a shimmer on non-terminal rows (`RecentRunsTable.tsx`), sparkline bars draw in staggered (`Sparkline.tsx`, shared with Evaluations), topbar status dot pulses (`TopBar.tsx`). Trace viewer: step cards stagger-mount with a connector line that grows in, `springBouncy` expand/collapse, JSON panels crossfade+rise, running badge gets an opacity-only breathing glow (`StatusBadge.tsx`), success reveal draws an animated SVG checkmark (`pathLength`) next to the status badge, failure panels (run-level + per-step) do a once-only 300ms shake (`StepCard.tsx`, `TraceHeader.tsx`, `runs/[id]/page.tsx`). Workflow builder: bouncier reorder spring, type-icon hover micro-rotation, add/remove unchanged (`StepEditor.tsx`); Save button crossfades to a checkmark + "Saved" on success (`WorkflowForm.tsx`). Workflows list: cards lift + glow the accent border on hover (`WorkflowCard.tsx`). Runs list: same row-cascade + shimmer pattern as the dashboard (`runs/page.tsx`). Evaluations: pass-rate/total tiles count up, result rows cascade (`evaluations/page.tsx`). Sidebar: nav icons micro-scale on hover via CSS `group-hover`/`motion-reduce:` (`Sidebar.tsx`). No visible text, aria roles, or test-relevant semantics changed. Verified: `npx tsc --noEmit` clean, `npm run lint` clean, `npx vitest run` 135/135 passing, `npm run build` succeeds, all 6 routes curl 200, and a headless Playwright pass through all 6 pages + step-card expand + workflow detail produced zero console/page errors. `docs/UX_FLOW.md` "Intensified pass 2026-07-12" subsection added. (Phase 9, frontend-engineer)

- 2026-07-12 [feat] Playwright e2e test of the guided demo path (FINAL_HANDOFF.md improvement #3): `app/e2e/demo-path.spec.ts` + `app/playwright.config.ts` run the exact recruiter demo flow — dashboard loads → "Run demo workflow" resolves the triage pipeline with a prefilled JSON sample input → submit navigates to the trace page → poll to `Awaiting Approval` → expand the "Human Review" step and click Approve → poll to `Succeeded` → assert all 6 trace steps and at least one evaluation result — against a live local stack (dev server + worker + Docker Postgres/Redis; no `webServer` auto-start, a clear `beforeAll` reachability check fails fast if `:3000` is down instead of timing out opaquely). `@playwright/test` added as a devDependency (matches the already-installed `playwright`/Chromium version used by `scripts/capture-screenshots.mjs`, no new browser download); `npm run test:e2e` script added; `vitest.config.ts` `exclude` extended with `e2e/**` so Vitest never picks up Playwright specs. `test.describe.configure({ retries: 1 })` absorbs the documented ~0.2% residual mock-provider full-run failure rate without softening assertions. Selectors are roles/accessible-names/visible-text only, no CSS/`nth-child`, no animation-timing assertions. Verified live: `npx playwright test` 1/1 passing (~8-10s, run twice back-to-back); `npx vitest run` 135/135 passing across 20 files (unaffected); `npx tsc --noEmit` clean. `npm run lint` has one pre-existing error + a few warnings from concurrent, unrelated animation-agent edits to `src/lib/motion.ts` / `src/app/runs/page.tsx` (outside this task's file ownership; not introduced or fixed here). `docs/TEST_PLAN.md` updated with the e2e section (how to run, stack prerequisites, what's asserted) and coverage map entry. (Phase 10/11, qa-tester)

- 2026-07-12 [feat] Real LLM provider seam (free-tier only, human checkpoint cleared): `app/src/lib/providers/openaiCompatProvider.ts` implements `Provider` via plain `fetch` against any OpenAI-compatible `/chat/completions` endpoint (no vendor SDK) — one implementation covers Google Gemini's OpenAI-compat endpoint, Groq, and OpenRouter free models. `getProvider()` (`app/src/lib/providers/index.ts`) gained `PROVIDER_MODE="live"`, constructing it from `LLM_BASE_URL`/`LLM_API_KEY`/`LLM_MODEL` with a config error listing exactly which env var(s) are missing; `PROVIDER_MODE` still defaults to `"mock"` (zero keys required to build/test/run). Errors map to the runner's existing retry seam: network errors, timeouts (30s default via `AbortController`), and 408/429/5xx → `ProviderTransientError` (retried); 400/401/403 → a permanent error that never includes the API key. `app/.env.example` gained a commented `LLM_*` block with three ready-to-uncomment free-tier presets (Gemini, Groq, OpenRouter) and links to get each free key. Tests in `app/src/lib/__tests__/openaiCompatProvider.test.ts` use an injected `fetchImpl` — no real network calls. 135/135 Vitest passing, `npx tsc --noEmit` and `npm run lint` clean. `docs/ARCHITECTURE.md` and `docs/DECISIONS.md` updated. (Phase 9, backend-engineer)

- 2026-07-12 [fix] Workflow builder step-config editors now write the exact keys the executors read, closing a builder-vs-runtime config drift that made builder-created `llm_prompt`/`eval` steps fail and made seeded `llm_prompt` prompts render empty: `src/components/workflows/StepEditor.tsx` renamed the `llm_prompt` field from `prompt` → `promptTemplate` (matches `llmPrompt.executor.ts`); `tool_api` gained a GET/POST method `<select>` alongside the existing URL field (`toolApi.executor.ts` contract: `{url, method?}`); the `eval` editor was rebuilt from a single free-text `criteria` box into a real `evaluatorType` select (`deterministic|rubric|llm_judge`) with per-type fields — deterministic gets a JSON textarea for `checks` with client-side `JSON.parse` validation and an inline error message (invalid JSON is never silently written to config), rubric gets a comma-separated criteria input + optional threshold, llm_judge gets a rubric text field + optional threshold — matching `eval.executor.ts` + `src/lib/evaluators/*`. `updateConfig` continues to spread the step's existing config and overlay only the edited key, so keys the form doesn't render (e.g. a seeded step's `model`, `temperature`, `timeoutMs`, `requiredRole`, `description`) survive an edit — verified by inspection of `WorkflowForm.tsx`'s `config: s.config` initial load (full object, not filtered) and `StepEditor.tsx`'s `{ ...next[index].config, [key]: value }` merge. Also fixed `src/lib/format.ts` `formatCost`: sub-$0.01 mock-provider costs (e.g. `0.00005`) were rendering as the meaningless `"$0.0000"`; now `>= $0.01` shows 2dp, `>= $0.0001` shows 4dp, smaller non-zero values show `"<$0.0001"` instead of a false-zero-looking string. Extended `src/lib/__tests__/format.test.ts` for the new cost tiers. 117/117 Vitest passing, `npx tsc --noEmit` and `npm run lint` clean. Live-verified against the running dev server + worker: created a workflow via `POST /api/workflows` shaped exactly as the fixed builder now emits (all 5 step types incl. an `eval` step with `evaluatorType: "rubric"`), ran it via `POST /api/runs`, approved at the `approval` checkpoint via `POST /api/runs/{id}/approve`, and confirmed it reached `status: "succeeded"` with a persisted `EvaluationResult` row (`evaluatorType: "rubric"`, `score: 0.785`, `passed: true`). Separately `GET`-verified a seeded workflow returns `promptTemplate` (not `prompt`) on its `llm_prompt` steps, confirming the editor's read path now displays seeded prompts instead of blank fields. (Phase 12/9, frontend-engineer)

- 2026-07-12 [docs] Final documentation pass (Phase 14/16): `app/README.md` rewritten from the create-next-app default into the recruiter-facing centerpiece (features, ASCII architecture diagram, tech stack, verified quickstart, 2-minute demo script, testing section, honest limitations); fixed doc drift found by the architect review — `docs/ARCHITECTURE.md` project-layout tree (`src/core/` → `src/lib/` + `src/worker/index.ts`) and `npm run seed` → `npm run db:seed`, latency wording clarified (Run.latencyMs = sum of step execution latencies, excludes approval dwell) in `docs/ARCHITECTURE.md` + `docs/DATA_MODEL.md`; `docs/API_SPEC.md` documented the optional `details` field on validation errors, `successRate` semantics (excludes canceled), and the `recentRuns` limit of 10; `docs/DEPLOYMENT.md` and `docs/TEST_PLAN.md` also corrected for the same `src/lib`/port-5433/`db:seed`/actual-test-file drift. Added `FINAL_HANDOFF.md` at the repo root (capabilities vs. requirements, verified commands + live metrics, demo script, honest not-done list including the Framer Motion skill substitution, next-3 recommendations, 4 resume bullets). `docs/HANDOFF.md` now points to `FINAL_HANDOFF.md` with a short pointer + ship verdict. `tasks/NEXT_ACTIONS.md` and `tasks/SPRINT_PLAN.md` updated to reflect Sprints 1–4 complete. (Phase 14/16, documentation-writer)

- 2026-07-11 [feat] Backend execution engine + API (Sprints 1-2, T2-T14): `src/lib/types.ts` shared domain/API types; `src/lib/providers/` deterministic-but-varied `MockLlmProvider` (simulated 150-1200ms latency, ~8% transient-failure rate, injectable rng/latency for tests); `src/lib/executors/` for all 5 step types (llm_prompt, tool_api against a mock tool registry, transform with a safe path/JSON.parse DSL — no `eval()`, approval, eval); `src/lib/evaluators/` deterministic (contains/regex/json_valid/length), rubric (weighted-average scoring math + provider-judged criteria), llm_judge placeholder; `src/lib/runner.ts` sequential orchestrator with resume-from-first-non-terminal-step, up to 2 retries on transient llm_prompt failures, pause-and-halt on approval, stop-on-failure with remaining steps marked `skipped`; `src/lib/queue.ts` + `src/worker/index.ts` BullMQ queue/worker with graceful shutdown; full REST API under `src/app/api/**` (workflows CRUD, runs create/list/detail/cancel, approve/reject, evaluations list, dashboard aggregates) with zod validation and the `{ error: { code, message } }` envelope. 62 Vitest unit tests passing (`npx vitest run`); `npx tsc --noEmit` and `npm run lint` clean on all backend-owned files. Verified end-to-end against Docker Postgres/Redis: created a run, worker executed it through both eval steps (deterministic + llm_judge) and persisted `EvaluationResult` rows; separately verified the approval pause → `POST /approve` → resume → rubric eval path, plus reject, cancel (best-effort race with the worker is expected/documented), and workflow CRUD incl. cascade delete. (Phase 9, T2-T14)

- 2026-07-11 [fix] Backend bugfix pass (B4, B6, B8, B9, B10): `evaluateDeterministic` now throws a clear validation error instead of vacuously passing (score 1) when `config.checks` is missing/empty; `prisma/seed.ts` summarizer's deterministic step now emits the canonical `{checks:[...]}` shape and its "Generate Title" prompt now references `{{steps[0].output.raw}}` (the actual llm_prompt output key) instead of a nonexistent `.summary` field; `src/lib/runner.ts` guards all run-status writes (start-running, awaiting_approval, failed, succeeded) with `updateMany({status:{notIn:terminal}})` and bails out on a 0-row match, closing the cancel-vs-worker-start TOCTOU race that could resurrect a canceled run; `GET /api/runs` now clamps `limit` to [1,200] instead of rejecting out-of-range values with 400 (API_SPEC.md updated to match); `GET /api/dashboard` `successRate` is now `succeeded / (succeeded + failed)`, excluding canceled runs from the denominator. Extended/added tests in `evaluators.deterministic.test.ts`, `runner.test.ts` (new race-condition test), `runs.route.test.ts`, `dashboard.route.test.ts` — 114/114 Vitest passing, `tsc --noEmit` and `npm run lint` clean, `npm run db:seed` re-runs cleanly. Live-verified B4 (real `checks` array in persisted EvaluationResult) and B8 (non-empty summary text flows into the title step) against the running dev server + worker. (Phase 12, B4/B6/B8/B9/B10)

- 2026-07-11 [feat] Frontend: full UI per docs/UX_FLOW.md — dark developer-tool app shell (sidebar/topbar), Dashboard with metric cards + recent runs + eval summary, Workflows list + builder (add/reorder/remove steps, per-type config, save/run), Runs list with status filters + live polling, flagship Run trace viewer (expandable step timeline, JSON I/O panels, inline approve/reject, eval results), Evaluations view (pass rate, score sparkline, expandable results). Design tokens + Framer Motion system in `globals.css`/`src/lib/motion.ts` per the UX_FLOW visual/motion spec; every screen has loading/empty/error states with retry. (Phase 9, T-frontend)

## 2026-07-13 — Live provider verified on Gemini free tier
- PROVIDER_MODE=live verified end-to-end (gemini-flash-lite-latest): real classification JSON, context-aware draft reply, approval pause/resume, real rubric judge scores.
- Real-LLM fixes the mock had masked: seed classify/draft prompts (JSON demand + context interpolation), judge prompts demand raw JSON, code-fence-tolerant JSON.parse in transform executor + both judges, seed steps no longer pin a mock-only model name, provider 404 error names the model tried.
- Default remains mock (keyless demo); README/DECISIONS updated.
